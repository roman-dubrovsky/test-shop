h1 Product kind editing

= simple_form_for [:admin, model], wrapper: :horizontal_form, html: {class: "form-horizontal"} do |f|
  = f.input :title

  = f.submit "Save", class: "btn btn-success btn-lg"
